# requirements-email.txt
# Minimal deps for the daily prediction email job.
# Keep separate from the main retrain/collect requirements to keep the job fast.
xgboost>=2.0
pandas>=2.0
numpy>=1.26
requests>=2.31
